<section class="featured-products">
    <div class="container">
        <h3> {{ section.settings.heading }} </h3>
        <div class="row">
            {% for product in collections[section.settings.golf_collection].products %}
                {% render 'custom-product-card', card_product : product %}    
            {% endfor %}
        </div>
    </div>
</section>

{% schema %}
{
    "name" : "Featured Collection",
    "settings" :
    [
       {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "Featured Collection"
       },
       {
        "type": "collection",
        "id": "golf_collection",
        "label": "Collection"
       }
    ],
    "presets":
    [
       {
            "name": "Featured Golf Collection",
            "category": "Custom Sections"
       }
    ]
}
{% endschema %}